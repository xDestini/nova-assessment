## Notes

This project uses AI-assisted development tools (Cursor, Warp Agent)
as required. The final implementation decisions and refinements were made manually.

Note: pnpm is installed in both build and runtime stages for simplicity and clarity in the assessment context.
